# 🟢 Shape Detector (OpenCV + TypeScript + Vite)

A simple web application that detects geometric shapes (like circles, rectangles, triangles) in an uploaded image using **OpenCV.js**.  
Built with **TypeScript**, **Vite**, and **HTML Canvas**.

---

## 🚀 Features

- Upload an image directly from your computer  
- Automatically detects and labels shapes (Triangle, Rectangle, Circle, etc.)  
- Uses OpenCV.js for edge and contour detection  
- Fast and runs entirely in your browser — no backend required  

---

## 🧠 Tech Stack

- **Frontend:** Vite + TypeScript  
- **Library:** OpenCV.js (`@techstark/opencv-js`)  
- **Rendering:** HTML Canvas  

---

## ⚙️ Setup & Installation

### 1️⃣ Clone the repository
```bash
git clone https://github.com/homingos/shape-detector.git
cd shape-detector



